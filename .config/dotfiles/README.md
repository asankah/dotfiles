# Updating and maintaining configuration

The configuration files here are maintained using `vcsh`. The binary is
available on all major Linux distros as well as Homebrew (on both Linux and
macOS). Install it via:

```sh
brew install vcsh
```

This file is in the public dotfiles repository at
git@github.com:asankah/dotfiles.git


